Create or update a `TokenSpec` for the current task.

Arguments: $ARGUMENTS

## Your job

Generate a semantic style system for the requested UI.

## Must include

- themes
- semantic color tokens
- typography tokens
- spacing scale
- radius scale
- motion tokens
- density
- contrast
- geometry
- texture
- ornament
- anti-generic constraints

## Rules

- avoid cliché AI styles unless explicitly requested
- prefer semantic token names over raw values
- ensure light/dark theme compatibility
- ensure accessibility-aware contrast intent
- do not emit component code here
