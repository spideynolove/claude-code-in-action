Switch to plan-only output mode for this session.

For the remainder of this session:
- Respond to all task requests with a concise plan only (≤7 bullets), labeled `[PLAN]`
- Do not generate detailed plans or code
- Wait for explicit `/implement` before writing any code
- Wait for explicit `/detailed-plan` before expanding the plan
